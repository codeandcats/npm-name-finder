// import { cssRule } from 'typestyle';
import { hsla, percent } from 'csx';

export const npmRed = hsla(0, percent(59), percent(51), 1).toString();

// cssRule('.btn.btn-primary', {
//   backgroundColor: npmRed
// });
